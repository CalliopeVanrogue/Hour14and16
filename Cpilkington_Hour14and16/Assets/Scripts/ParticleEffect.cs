using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleEffect : MonoBehaviour
{
    

// Size of the particle effect
    // shape of the particle effect (circle instead of cone)
    // Rate of particles
    // color of particles including a fade as they fall
    //randomized directions of particles

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
